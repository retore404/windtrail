"use client";

import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#546c95",
      light: "#c0cada",
      dark: "#203965",
    },
    secondary: {
      main: "#d2b762",
      light: "#fbe280",
      dark: "#957d54",
    },
  },
});

export default theme;
